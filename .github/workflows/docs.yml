name: Generate PDFs

permissions:
  contents: write

on:
  push:
    paths:
      - 'docs/**'
      - 'Makefile'
      - '.github/workflows/docs.yml'
  pull_request:
    branches: [main]
    paths:
      - 'docs/**'
      - 'Makefile'
      - '.github/workflows/docs.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: pandoc/extra:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate PDFs
        run: |
          PANDOC_FLAGS="--template eisvogel --pdf-engine=xelatex --toc --toc-depth=3 --number-sections"
          pandoc docs/markdown/conceptual-design.md -o docs/pdf/conceptual-design.pdf $PANDOC_FLAGS --metadata-file=docs/markdown/metadata/design.yaml
          pandoc docs/markdown/logical-design.md -o docs/pdf/logical-design.pdf $PANDOC_FLAGS --metadata-file=docs/markdown/metadata/design.yaml
          pandoc docs/markdown/physical-design.md -o docs/pdf/physical-design.pdf $PANDOC_FLAGS --metadata-file=docs/markdown/metadata/design.yaml
          pandoc docs/markdown/deliver.md -o docs/pdf/deliver.pdf $PANDOC_FLAGS --metadata-file=docs/markdown/metadata/deliver.yaml
          pandoc docs/markdown/operate.md -o docs/pdf/operate.pdf $PANDOC_FLAGS --metadata-file=docs/markdown/metadata/operate.yaml

      - name: Upload PDFs as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dda-vcf-docs
          path: docs/pdf/*.pdf
          retention-days: 90

  commit:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download PDFs
        uses: actions/download-artifact@v4
        with:
          name: dda-vcf-docs
          path: docs/pdf/

      - name: Commit PDFs
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: regenerate PDFs [skip ci]"
          file_pattern: "docs/pdf/*.pdf"

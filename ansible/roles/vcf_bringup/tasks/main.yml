---
# VCF management domain bringup via Cloud Builder API

# --- Depot configuration and bundle download ---
# The VCF Installer needs bundles downloaded from the depot before
# validation or bringup can proceed.

- name: Get VCF Installer API token
  ansible.builtin.uri:
    url: "https://{{ vcf_bringup_hostname }}/v1/tokens"
    method: POST
    body_format: json
    body:
      username: "{{ vcf_bringup_username }}"
      password: "{{ vcf_bringup_password }}"
    validate_certs: false
    status_code: 200
  register: token_response

- name: Configure VCF depot
  ansible.builtin.uri:
    url: "https://{{ vcf_bringup_hostname }}/v1/system/settings/depot"
    method: PUT
    headers:
      Authorization: "Bearer {{ token_response.json.accessToken }}"
    body_format: json
    body:
      offlineAccount:
        username: "{{ depot_username }}"
        password: "{{ depot_password }}"
      depotConfiguration:
        isOfflineDepot: true
        hostname: "{{ vcf_depot_url }}"
        port: 443
    validate_certs: false
    status_code: [200, 202, 204]
    timeout: 300
  register: depot_result

- name: Display depot configuration result
  ansible.builtin.debug:
    msg: "Depot configured: {{ depot_result.status }}"

# Bundle download is a manual step via the VCF Installer UI (see deliver guide §6.2.1).
# Automation tracked in GitHub issue #80.

# --- Pre-validation: vSAN ESA HCL bypass and timestamp patch ---
# Nested ESXi uses virtual NVMe disks not on the vSAN HCL. VCF 9.0.1+
# has a built-in bypass via the domainmanager property below. Also patch
# the HCL timestamp in case the OVA is > 90 days old.
#
# These tasks delegate to the vcf-installer host via Ansible's native
# SSH (ProxyJump through gateway) as the vcf user with sudo. sshpass
# doesn't work with the VCF Installer because its SSH welcome banner
# confuses sshpass's password prompt detection. Root SSH is disabled.

- name: Check if vSAN ESA HCL bypass is already configured
  ansible.builtin.shell: >
    grep -q 'vsan.esa.sddc.managed.disk.claim=true'
    /etc/vmware/vcf/domainmanager/application-prod.properties
  delegate_to: vcf-installer
  become: true
  vars: &vcf_installer_ssh
    ansible_user: vcf
    ansible_password: "{{ vcf_bringup_password }}"
    ansible_become_password: "{{ vcf_bringup_password }}"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ControlMaster=no -o ProxyJump=ubuntu@{{ gateway_public_ip }}"
  register: hcl_check
  failed_when: false
  changed_when: false

- name: Add vSAN ESA HCL bypass property
  ansible.builtin.shell: >
    echo 'vsan.esa.sddc.managed.disk.claim=true' >>
    /etc/vmware/vcf/domainmanager/application-prod.properties
  delegate_to: vcf-installer
  become: true
  vars: *vcf_installer_ssh
  when: hcl_check.rc != 0

- name: Restart domainmanager service
  ansible.builtin.shell: systemctl restart domainmanager
  delegate_to: vcf-installer
  become: true
  vars: *vcf_installer_ssh
  when: hcl_check.rc != 0

- name: Patch vSAN HCL timestamp
  ansible.builtin.shell: |
    NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ)
    sed -i "s/\"timestamp\":\"[^\"]*\"/\"timestamp\":\"$NOW\"/" /nfs/vmware/vcf/nfs-mount/vsan-hcl/all.json
    sed -i "s/\"jsonUpdatedTime\":\"[^\"]*\"/\"jsonUpdatedTime\":\"$NOW\"/" /nfs/vmware/vcf/nfs-mount/vsan-hcl/all.json
  delegate_to: vcf-installer
  become: true
  vars: *vcf_installer_ssh
  failed_when: false
  changed_when: true

- name: Wait for VCF Installer API to be ready
  ansible.builtin.uri:
    url: "https://{{ vcf_bringup_hostname }}/v1/tokens"
    method: POST
    body_format: json
    body:
      username: "{{ vcf_bringup_username }}"
      password: "{{ vcf_bringup_password }}"
    validate_certs: false
    status_code: 200
  register: api_ready
  until: api_ready.status == 200
  retries: 24
  delay: 10

# --- Bringup validation and deployment ---

- name: Display bringup spec being submitted
  ansible.builtin.debug:
    msg: "{{ vcf_bringup_spec | to_nice_json }}"

- name: Validate bringup spec
  vcf_cloud_builder:
    hostname: "{{ vcf_bringup_hostname }}"
    username: "{{ vcf_bringup_username }}"
    password: "{{ vcf_bringup_password }}"
    state: validated
    spec: "{{ vcf_bringup_spec }}"
  register: validation_result

- name: Display validation result
  ansible.builtin.debug:
    var: validation_result

- name: Start VCF bringup
  vcf_cloud_builder:
    hostname: "{{ vcf_bringup_hostname }}"
    username: "{{ vcf_bringup_username }}"
    password: "{{ vcf_bringup_password }}"
    state: deployed
    spec: "{{ vcf_bringup_spec }}"
    timeout: 7200
    poll_interval: 60
  register: bringup_result

- name: Display bringup result
  ansible.builtin.debug:
    var: bringup_result

- name: Verify management components are accessible
  ansible.builtin.uri:
    url: "https://{{ item }}.{{ lab_domain }}"
    validate_certs: false
    status_code: [200, 301, 302]
  loop:
    - vcenter-mgmt
    - sddc-manager
    - nsx-mgr-mgmt
  retries: 10
  delay: 30
  register: component_check
  until: component_check.status in [200, 301, 302]

---
# Phase 5 — Replace VCF component certificates with step-ca issued certs
#
# TODO: Implement certificate replacement workflow:
#   1. Import step-ca root cert as trusted CA in SDDC Manager
#   2. Generate CSRs for vCenter, NSX Manager, SDDC Manager
#   3. Sign CSRs with step-ca on the gateway
#   4. Install signed certificates via SDDC Manager API
#
# The vcf_certificate collection module supports:
#   - create_ca: Import CA certificate
#   - generate_csrs: Generate CSRs for a domain's resources
#   - install_certificates: Install signed certificates
#
# Blocked: Requires research on step-ca CSR signing workflow
#   and SDDC Manager certificate API spec format.

- name: CA certificate replacement (placeholder)
  ansible.builtin.debug:
    msg: >
      Certificate replacement not yet implemented.
      VCF components are using self-signed certificates.
      See deliver.md for manual certificate procedures.

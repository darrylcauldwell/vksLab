---
# Phase 1 — Foundation: gateway services, Docker containers, routing, BGP
- name: Phase 1 — Configure gateway
  hosts: gateways
  gather_facts: true
  become: false
  roles:
    - gateway
    - docker_services

---
# Phase 1 — Foundation: jumpbox services, Docker containers, routing, BGP
- name: Phase 1 — Configure jumpbox
  hosts: jumpboxes
  gather_facts: true
  become: false
  roles:
    - jumpbox
    - docker_services
